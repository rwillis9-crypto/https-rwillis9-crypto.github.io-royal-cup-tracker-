name: Daily Email Recap

on:
  schedule:
    - cron: "30 12 * * 1-5"
  workflow_dispatch:

jobs:
  email:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: echo "It works"
